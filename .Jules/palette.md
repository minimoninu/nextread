## 2024-06-18 - Icon-only buttons accessibility
**Learning:** Depending exclusively on the `title` attribute for icon-only buttons is insufficient for optimal accessibility.
**Action:** Always include both `aria-label` (for screen readers) and `title` (for desktop users) on interactive icon-only elements.
